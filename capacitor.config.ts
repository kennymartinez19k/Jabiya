import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'jabiya-ionic-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
